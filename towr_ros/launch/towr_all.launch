<launch>

  <!-- so the logging print the node name -->
  <env name="ROSCONSOLE_FORMAT" value="[${severity}] [${node}]: ${message}" />

  <!-- Upload anymal URDF and IK-->
  <include file="$(find xpp_vis)/launch/xpp_full.launch"></include>
  
  <!-- Start the visualization -->
  <include file="$(find towr_ros)/launch/towr_vis.launch"></include>
  
  <!-- Start the optimizer-->
  <include file="$(find towr_ros)/launch/towr_core.launch"></include>
       
</launch>